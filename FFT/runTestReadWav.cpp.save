#include<iostream>
#include<fstream>
#include "readWav.h"
using namespace std;
int* buff;
int buffLength = 1000;

int main(int argc, char *argv[])
{
WavReader wvRdr;
File *input = fopen(argv[1], "r");
if (!filp) {printf("Error: could not open file %s\n",input); return -1;}
input.open(argv[1],std::ifstream::in);
wvRdr.init();

while(!input.eof())
{
wvRdr.ReadWav(input,buff,buffLength);
}
return 0;
}

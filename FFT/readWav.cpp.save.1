#include <cstdio>
//#include<iostream>
#include<fstream>
#include "readWav.h"
using namespace std;

WavReader::WavReader()
{
int m_bufflength = -1;
int m_maxdur = 1000;
}

WavReader::~WavReader()
{

}

int WavReader::init()
{
this->m_buffLength = -1;
this->m_maxdur = 1000;
}

int WavReader::GetWavInfo(File *iFile)
{
//////METHOD TO GET SAMPLE RATE///////////
int bitDepth = 8;
	std::cout << "Bit Depth = " << std::cout <<bitDepth;
return 0;
}

int WavReader::ReadWav(File *iFile, int *buffer, int buffLength)
{
cout<<"ReadWav run"<<endl;
        unsigned int n = 0;

	//Open an input and output stream in binary mode
	if(iFile.is_open())
	{
	cout<<"File is open"<<endl;
	cout<<"m_maxdur="<<m_maxdur<<endl;
	while(n < m_maxdur)
	{
			buffer[n]= iFile.get();
			cout <<n<<endl;//":"<<buffer[n]<<" ";
			n++;
	}
	}
	else
	{
	cout<<"Not opened"<<endl;
	}

//	for(n=0; n<m_maxdur; n++)
//	{
//		cout<<buffer[n]<<" ";
//	}
return 0;

}
